{% extends 'base.html.twig' %}
{% block content %}
    <ul role="list" class="divide-y divide-gray-100">
        {% for character in characters %}
            {% include 'parts/character-index.html.twig' with {'character': character} %}
        {% endfor %}
    </ul>
{% endblock %}
