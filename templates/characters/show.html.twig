{% extends 'base.html.twig' %}
{% block content %}
    {# TODO: add search #}
    {# TODO: add table overview component for location data #}
    {{ dump(character) }}
    {% for person in character %}

        <b>{{ loop.index }}</b>{{ person.name }} <br>
        {#        {{ dump(location) }} #}
    {% endfor %}
    {#    {{ dump(pagination) }} #}
    {# TODO: add pagination #}
{% endblock %}
    {# TODO: add secondary column #}
