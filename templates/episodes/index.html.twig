{% extends 'base.html.twig' %}
{% block content %}
    {# TODO: add search #}
    {# TODO: add table overview component for episode data #}
    {{ dump(episodes) }}
    {% for episode in episodes %}
        {{ episode.name }} <br>
        {{ dump(episode) }}
    {% endfor %}
    {% include 'parts/pagination.html.twig' with {'pagination': pagination, 'paginationPath': 'episodes'} %}
{% endblock %}
