{% extends 'base.html.twig' %}
{% block content %}
    {{ dump(location) }}
    {% for character in characters %}

        <b>{{ loop.index }}</b>{{ character.name }} <br>
        {#        {{ dump(location) }} #}
    {% endfor %}
{% endblock %}
