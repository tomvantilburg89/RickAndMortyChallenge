{% extends 'base.html.twig' %}
{% block content %}
    <ul role="list" class="divide-y divide-gray-100">
        {% for location in locations %}
            {% include 'parts/location-index.html.twig' with {'location': location} %}
        {% endfor %}
    </ul>
    {% include 'parts/pagination.html.twig' with {'pagination': pagination, 'paginationPath': 'locations'} %}
{% endblock %}
